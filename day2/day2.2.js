const day2 = (input) => {
  // const inputArr = input.split("\n\n").map((el) => el.split("\n"));
//   const sumOfElf = inputArr.map((elf) =>
//   elf.reduce((acc, cur) => {
//     acc += parseInt(cur);
//     return acc;
//   }, 0)
//   );
//  return Math.max(...sumOfElf)
};

module.exports = day2;
